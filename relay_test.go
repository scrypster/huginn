package main

// NOTE: TestCmdRelayRegister was removed because cmdRelayRegister now uses
// a browser-based or device-code registration flow (via reg.Register())
// that requires network interaction and cannot be easily unit tested.
// Integration tests should verify this functionality against a live server.
